{
  "scripts": {
    "dev": "parcel serve test/index.html --out-dir tmp/",
    "build": "parcel build src/index.js --out-dir dist/ --global EmojiExploder",
    "build-test": "parcel build test/index.html --out-dir test_built/"
  },
  "devDependencies": {
    "cssnano": "^4.1.10",
    "parcel": "^1.12.3",
    "sass": "^1.22.7"
  },
  "dependencies": {
    "matter-js": "^0.14.2",
    "random-emoji": "^1.0.2"
  }
}
